// Input variables are declared with the first governed resource change for
// this area. The organization instance supplies every concrete value as
// reviewed configuration; the core never carries an organization default.
